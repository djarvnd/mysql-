Download MYSQL RPM
wget http://repo.mysql.com/mysql-community-release-el7-5.noarch.rpm
Installs repo file 
sudo rpm -ivh mysql.rpm
sudo yum -y install mysql-server
sudo systemctl start mysqld
Starts on port 3306
ps -ef | grep mysql
sudo mysql_secure_installation
When running for first time, hit Enter button then set the password by hitting Y
sudo mysql -u root -p
show databases
use database;
